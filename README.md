# RP01 — Bayt-Style

Repository bootstrap anchor for `RP01` / `RP01-WS-REPO-GOV-BOOTSTRAP`.

This exceptional owner-authorized initial commit exists only to establish the `main` branch in a previously empty repository. It introduces no product implementation, no imported Stage A–D artifacts, no gate decision, and no merge or release authorization.
